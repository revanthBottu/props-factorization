task: cont_space_llm_num_optim
num_episodes: 5  # Short test run
gym_env_name: CartPole-v1
render_mode: null
logdir: logs/cartpole_lu_test
dim_actions: 2
dim_states: 4
max_traj_count: 1000
max_traj_length: 500
template_dir: agent/policy/templates
llm_si_template_name: num_optim_lu.j2  # Use LU template
llm_output_conversion_template_name: num_optim.j2
llm_model_name: gemini-2.0-flash-exp  # Fast model for testing
num_evaluation_episodes: 3  # Reduced for testing
warmup_episodes: 2  # Reduced for testing
warmup_dir: null
bias: true
optimum: 500
search_step_size: 0.1
use_factorized_policy: true  # Enable factorized policy
factor_rank: 2  # Inner dimension for L @ U (4 states, 2 actions, so 2 is good)
# frozen_factor: null | "L" | "U"  (null = both updated; "L" = freeze L; "U" = freeze U)
frozen_factor: null
